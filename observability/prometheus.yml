global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: mineiq-ingestion
    metrics_path: /prometheus
    static_configs:
      - targets: ["ingestion-service:8000"]
        labels:
          service: ingestion
